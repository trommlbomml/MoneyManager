<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:MoneyManager.ViewModels;assembly=MoneyManager.ViewModels"
                    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">
    
    <DataTemplate DataType="{x:Type viewModels:RequestManagementScreenModel}">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal">
                <Button Height="64" Width="64" Content="Add" Command="{Binding AddRequestCommand}" />
                <Button Height="64" Width="64" Content="Remove" Command="{Binding DeleteRequestCommand}" />
                <Button Height="64" Width="64" Content="Remove" Command="{Binding SaveCommand}" />
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Grid.Row="1">
                
                <Label Content="Jahr:" Margin="2" />
                <xctk:LongUpDown Value="{Binding Year}" Width="150" Margin="2" />

                <Label Content="Monat:" Margin="2" />
                <xctk:LongUpDown Value="{Binding Month}" Width="150" Margin="2" Minimum="0" Maximum="11" />

            </StackPanel>
            
            <DataGrid Grid.Row="2" 
                      AutoGenerateColumns="False" 
                      ItemsSource="{Binding Requests}"
                      SelectedItem="{Binding SelectedRequest}"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Datum" 
                                        Binding="{Binding DateAsString}" 
                                        Width="Auto" />
                    <DataGridTextColumn Header="Beschreibung" 
                                        Binding="{Binding Description}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Betrag" 
                                        Binding="{Binding ValueAsString}"
                                        Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBox Grid.Row="3" IsReadOnly ="True" Text="{Binding SaldoAsString}" HorizontalAlignment="Right" Width="80" />
        </Grid>
        
    </DataTemplate>
    
</ResourceDictionary>